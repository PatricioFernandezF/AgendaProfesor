#include <stdlib.h>
#include "Usuario.h"
#include "fichero.h"

static void alta_alumno(Alumno*);
static void baja_alumno(Alumno*);
static void modifica_alumno(Alumno*);
static void listar_alumno(Alumno*);

// Cabecera: void alta_alumno(Alumno*)
// Precondicion: Recibe una cadena de caracteres de un alumno.
// Poscondicion:
static void alta_alumno(Alumno*)
{
}
// Cabecera: void baja_usuario(Alumno*)
// Precondicion: Recibe una cadena de caracteres de un alumno.
// Poscondicion:
static void baja_alumno(Alumno*)
{
}
// Cabecera: void modifica_usuario(Alumno*)
// Precondicion: Recibe una cadena de caracteres de un alumno.
// Poscondicion:
static void modifica_alumno(Alumno*)
{
}
// Cabecera: void listar_usuario(Alumno*)
// Precondicion: Recibe una cadena de caracteres de un alumno.
// Poscondicion:
static void listar_alumno(Alumno*)
{
}
